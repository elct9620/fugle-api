# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in fugle.gemspec
gemspec

gem 'rake', '~> 13.0'

gem 'rspec', '~> 3.0'

gem 'simplecov'

gem 'rubocop', '~> 1.22'
gem 'rubocop-rake'
gem 'rubocop-rspec'

gem 'bundler-audit'
gem 'overcommit'

gem 'pry'

gem 'vcr'
gem 'webmock'
